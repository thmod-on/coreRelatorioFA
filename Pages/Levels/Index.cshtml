@page
@model CoreRelatorioFA.Pages.Levels.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Level[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Level[0].Factor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Level[0].Contract)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Level) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Factor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Contract.Sap)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.LevelId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.LevelId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.LevelId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
